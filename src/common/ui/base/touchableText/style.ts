import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  largeText: {
    fontSize: 20,
    fontWeight: 'bold',
    color: '#00C2FF',
    marginTop: 15,
  },
  smallText: {
    fontSize: 18,
    fontWeight: 'bold',
    color: '#00C2FF',
    marginTop: 15,
  },
});

export default styles;
